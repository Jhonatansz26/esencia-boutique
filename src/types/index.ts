export interface Product {
  id: string;
  name: string;
  price: number;
  image: string;
  whatsappMessage: string;
}

export interface Review {
  id: string;
  name: string;
  rating: number;
  comment: string;
  date: string;
}
